---
description: Verification steps to run after modifying Python code
---
When you have finished changing a batch of Python scripts, you MUST run the unified verification script to ensure the codebase still satisfies Ruff, Pylint, and test coverage requirements.

// turbo-all
1. Run `python3 verify_all.py` in the root of the repository.
